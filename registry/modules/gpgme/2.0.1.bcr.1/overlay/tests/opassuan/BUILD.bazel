load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "t-command",
    srcs = ["t-command.c"],
    copts = [
        "-include",
        "config.h",
    ],
    deps = [
        "//src:libgpgme",
        "//config:config",
        "@libgpg-error//src:libgpg_error",
    ],
)
